# Git & Version Control Rule

## Explicit Permission Required
- **Never Git Commit or Push Without Explicit Permission**: NEVER run `git commit` or `git push` to any remote or branch without explicit user permission or a direct request from the user in their prompt.
- **Local Working Directory**: Keep changes in the working directory for the user to review. Do not stage, commit, or push unless specifically directed to do so.
